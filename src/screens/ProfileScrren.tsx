import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileScrren: React.FC<any> = ({ navigation }) => {
  return (
    <View>
      <Text>ProfileScrren</Text>
    </View>
  );
};

export default ProfileScrren;

const styles = StyleSheet.create({});
